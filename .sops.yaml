# SOPS creation rules for this repo. Any file matching the regex is encrypted to
# the Intent Eval Platform age recipient on `sops --encrypt`. The matching private
# key lives at ~/.config/sops/age/keys.txt (never committed).
creation_rules:
  - path_regex: secrets(\..*)?\.sops\.ya?ml$
    age: age1me3vkelljqe2u4zcagja9ru5fdpfpw72xmch39fwle2cr0yfr4cs8vr5d8
